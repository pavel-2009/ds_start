"""Учебная точка входа для CNN: модуль модели отделен от обучения и визуализации."""

# Печатаем сообщение о загрузке, полностью сохраняя наблюдаемое поведение исходного файла.
print('Загрузка CNN...')
# Импортируем основной класс CNN из базового модуля, где теперь находится математическое ядро модели.
from cnn_core import CNN
# Импортируем обучающие утилиты и демонстрационный сценарий из отдельного модуля, чтобы разделить ответственность по логике.
from cnn_training import run_demo, train
# Печатаем сообщение об успешной загрузке, как и в исходной версии файла.
print('CNN загружена успешно!')


# Запускаем демонстрационный сценарий только при прямом выполнении файла, чтобы при импорте не начиналось обучение.
if __name__ == '__main__':
    # Вызываем вынесенную демонстрацию, сохраняя прежнюю функциональность стартового скрипта.
    run_demo()
