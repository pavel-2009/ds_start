# Принудительно считаем Python-файлы текстовыми, чтобы Git и GitLab строили текстовые диффы даже после старых неудачных кодировок.
*.py text eol=lf diff=python
# Для Markdown тоже явно фиксируем текстовый режим и LF, чтобы поведение было предсказуемым на всех платформах.
*.md text eol=lf
